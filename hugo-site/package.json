{
  "name": "nikolaus-wolf-academic-site",
  "private": true,
  "scripts": {
    "build": "hugo --gc --minify && pagefind --site public"
  },
  "devDependencies": {
    "hugo-bin": "^0.161.0",
    "pagefind": "^1.5.0"
  }
}
